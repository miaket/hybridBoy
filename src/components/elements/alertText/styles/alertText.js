import {
  StyleSheet,
} from 'react-native';

import {
  Colors,
} from 'react-native/Libraries/NewAppScreen';

export default StyleSheet.create({
  alertText: {
    margin: 8,
    fontSize: 12,
    color: '#f44336'
  },
});